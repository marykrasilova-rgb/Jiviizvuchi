import fs from 'node:fs/promises';
import os from 'node:os';
import path from 'node:path';
import {spawn} from 'node:child_process';
import ffmpegPath from 'ffmpeg-static';

export const config={maxDuration:60};
const SUPABASE_URL='https://uecdlqlwsrqmocbpgiwj.supabase.co';
const SUPABASE_KEY='sb_publishable_QJ_4e8-BHl0gOZifGqdv1w_doFwpTlb';
const BUCKET='voice-recordings';
function encPath(p){return String(p).split('/').map(encodeURIComponent).join('/')}
function run(args,{capture=false}={}){return new Promise((resolve,reject)=>{const p=spawn(ffmpegPath,args);let err='';p.stderr.on('data',d=>{err+=d.toString();if(err.length>80000)err=err.slice(-80000)});p.on('error',reject);p.on('close',code=>code===0?resolve(capture?err:''):reject(new Error(err||`ffmpeg exited ${code}`)))});}
function ymd(d){return d.toISOString().slice(0,10)}
function addDays(d,n){const x=new Date(d);x.setUTCDate(x.getUTCDate()+n);return x}
function israelNowParts(){const f=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Jerusalem',year:'numeric',month:'2-digit',day:'2-digit'});return new Date(f.format(new Date())+'T12:00:00Z')}
function bounds(period){const today=israelNowParts();const start=addDays(today,period==='month'?-29:-6);return{start:ymd(start),endExclusive:ymd(addDays(today,1)),labelEnd:ymd(today)}}
async function sb(pathname,{token,method='GET',body,headers={}}={}){const r=await fetch(SUPABASE_URL+pathname,{method,headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${token}`,...headers},body});if(!r.ok){const t=await r.text();throw new Error(`Supabase ${r.status}: ${t.slice(0,500)}`)}return r}
async function signedUrl(token,storagePath){const r=await sb(`/storage/v1/object/sign/${BUCKET}/${encPath(storagePath)}`,{token,method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({expiresIn:600})});const j=await r.json(),u=j.signedURL||j.signedUrl;if(!u)throw new Error('Could not sign source audio');return u.startsWith('http')?u:SUPABASE_URL+'/storage/v1'+u}
async function upload(token,storagePath,buf){await sb(`/storage/v1/object/${BUCKET}/${encPath(storagePath)}`,{token,method:'POST',headers:{'content-type':'audio/mpeg','x-upsert':'true'},body:buf})}
async function expressiveOffset(input,duration,take,forced){if(forced||duration<=take+1)return Math.max(0,(duration-take)/2);const window=1.5;let log='';try{log=await run(['-hide_banner','-nostats','-i',input,'-vn','-af',`astats=metadata=1:reset=${window},ametadata=print:key=lavfi.astats.Overall.RMS_level`,'-f','null','-'],{capture:true})}catch{return Math.max(0,(duration-take)*.38)}const vals=[...log.matchAll(/pts_time:([\d.]+)[\s\S]*?lavfi\.astats\.Overall\.RMS_level=([-\d.]+)/g)].map(m=>({t:+m[1],v:+m[2]})).filter(x=>Number.isFinite(x.v));if(!vals.length)return Math.max(0,(duration-take)*.38);let best={score:-Infinity,t:0};for(const x of vals){if(x.t<.6||x.t>duration-.6)continue;const local=vals.filter(y=>Math.abs(y.t-x.t)<=take/2);const avg=local.reduce((a,b)=>a+b.v,0)/Math.max(1,local.length);const variation=local.reduce((a,b)=>a+Math.abs(b.v-avg),0)/Math.max(1,local.length);const score=avg+variation*.35;if(score>best.score)best={score,t:x.t}}return Math.max(0,Math.min(duration-take,best.t-take/2))}
export default async function handler(req,res){if(req.method!=='POST'){res.setHeader('Allow','POST');return res.status(405).json({error:'Method not allowed'})}const auth=String(req.headers.authorization||'');if(!auth.startsWith('Bearer '))return res.status(401).json({error:'Unauthorized'});const token=auth.slice(7),period=req.body?.period==='month'?'month':'week';let tmp;try{const ur=await sb('/auth/v1/user',{token}),user=await ur.json();if(!user?.id)return res.status(401).json({error:'Unauthorized'});const b=bounds(period),start=b.start+'T00:00:00+03:00',end=b.endExclusive+'T00:00:00+03:00';const q=new URLSearchParams({select:'id,created_at,duration_before,audio_path_before,expression_media_path,include_in_week_highlight',user_id:`eq.${user.id}`,or:'(modality.eq.voice,and(modality.is.null,audio_path_before.not.is.null))',created_at:`gte.${start}`,order:'created_at.asc'});q.append('created_at',`lt.${end}`);const rr=await sb('/rest/v1/voice_entries?'+q.toString(),{token});let rows=(await rr.json()||[]).filter(x=>x.audio_path_before||x.expression_media_path);const byDay=new Map();const dayOf=r=>new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Jerusalem',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date(r.created_at));for(const row of rows){const day=dayOf(row);if(!byDay.has(day))byDay.set(day,[]);byDay.get(day).push(row)}for(const group of byDay.values())group.sort((a,b)=>Number(b.include_in_week_highlight)-Number(a.include_in_week_highlight));const groups=[...byDay.values()];const limit=period==='month'?12:7;const picked=[];for(let round=0;picked.length<limit;round++){let added=false;for(let i=0;i<Math.min(groups.length,limit)&&picked.length<limit;i++){const index=groups.length>limit?Math.round(i*(groups.length-1)/(limit-1)):i;const row=groups[index][round];if(row){picked.push(row);added=true}}if(!added)break}rows=picked.sort((a,b)=>new Date(a.created_at)-new Date(b.created_at));if(rows.length<2)return res.status(422).json({error:'At least two voice recordings are needed',sourceCount:rows.length});tmp=await fs.mkdtemp(path.join(os.tmpdir(),'diary-montage-'));const segments=[];for(let i=0;i<rows.length;i++){const row=rows[i],source=row.audio_path_before||row.expression_media_path,url=await signedUrl(token,source),fr=await fetch(url);if(!fr.ok)throw new Error('Could not download source audio');const input=path.join(tmp,`in-${i}`);await fs.writeFile(input,Buffer.from(await fr.arrayBuffer()));const dur=Math.max(1,Number(row.duration_before)||10),take=Math.min(row.include_in_week_highlight?10:7,dur),offset=await expressiveOffset(input,dur,take,false),out=path.join(tmp,`seg-${i}.wav`),fadeOut=Math.max(0.2,take-0.45);await run(['-hide_banner','-loglevel','error','-ss',String(offset),'-i',input,'-t',String(take),'-vn','-ac','1','-ar','44100','-af',`highpass=f=70,loudnorm=I=-18:TP=-2:LRA=7,afade=t=in:st=0:d=0.25,afade=t=out:st=${fadeOut}:d=0.45`,'-y',out]);segments.push(out)}const list=path.join(tmp,'concat.txt');await fs.writeFile(list,segments.map(x=>`file '${x.replaceAll("'","'\\''")}'`).join('\n'));const output=path.join(tmp,'highlight.mp3');await run(['-hide_banner','-loglevel','error','-f','concat','-safe','0','-i',list,'-c:a','libmp3lame','-b:a','128k','-y',output]);const buf=await fs.readFile(output),storagePath=`${user.id}/highlights/${period}-${b.start}-${b.labelEnd}-${Date.now()}.mp3`;await upload(token,storagePath,buf);const payload={user_id:user.id,period_type:period,period_start:b.start,period_end:b.labelEnd,source_entry_ids:rows.map(x=>x.id),audio_path:storagePath,status:'ready',updated_at:new Date().toISOString()};const up=await sb('/rest/v1/diary_audio_highlights?on_conflict=user_id,period_type,period_start,period_end',{token,method:'POST',headers:{'content-type':'application/json',Prefer:'resolution=merge-duplicates,return=representation'},body:JSON.stringify(payload)}),saved=await up.json();return res.status(200).json({ok:true,highlight:Array.isArray(saved)?saved[0]:saved,sourceCount:rows.length,selection:'acoustic-expressiveness'})}catch(e){console.error(e);return res.status(500).json({error:'Audio montage failed',detail:String(e?.message||e).slice(0,900)})}finally{if(tmp)await fs.rm(tmp,{recursive:true,force:true}).catch(()=>{})}}
